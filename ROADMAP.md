# RepoLens 迭代路线图

> 原则:每个版本保持"纯前端、零依赖、零构建";每项功能上线前必须有自动化测试或浏览器实测覆盖。

## v2.1(本迭代,已执行)

目标:让核心卖点(评分)更**可信、可感知**,并建立回访动力。

### 1. 图表悬停提示
- [x] 热力图:hover 显示"日期 · N 次提交"
- [x] 月度趋势:hover 显示"N月 · M 次"
- [x] 雷达:hover 数据点显示"维度 分数/100"(对比雷达区分两个仓库)
- 实现:`charts.js` 内置全局浮层 `.chart-tip`,各图表注册命中检测闭包

### 2. API 配额指示器
- [x] 顶栏实时显示剩余配额(X-RateLimit-Remaining / Limit),绿/黄/红三档
- [x] 已配置 Token 时显示 🔑 标识
- 实现:`github.js` 每次响应后广播 `onRate` 回调,`app.js` 订阅更新

### 3. 历史与收藏
- [x] 分析成功后自动记入历史(去重、最新 20 条)
- [x] 洞察页一键收藏/取消收藏
- [x] 首页展示"⭐ 收藏"与"🕘 最近分析"卡片,可一键回访、可清空
- 实现:新增 `js/store.js` 存储层(localStorage),含独立单测 `test/store.test.mjs`

## v3 候选(按价值排序,未排期)

| 方向 | 说明 | 关键技术点 |
|---|---|---|
| 多仓库对比(2–4 个) | 选型常见 3–4 候选 | 雷达已天然支持叠加;深链扩展为 `a...b...c`;指标表改多列 |
| Star 增长曲线 | 比静态分数更有说服力 | star events 分页昂贵,考虑 `stargazers?per_page=100&accept=vnd.starred+json` 采样或外部数据源 |
| 评分权重调节 | 高级用户自定口径 | 滑杆 UI + 权重写 URL/query,重算即用 `score.js` 纯函数 |
| 图表单测 | v1 唯一严重 bug 出在绘图数据聚合 | 把日期聚合逻辑抽为纯函数,Node 直接测 |
| 数据防御 | 畸形响应不崩页面 | `analyze()` 前置字段校验,异常走错误卡 |
| i18n | 扩大受众 | 抽出文案字典;默认 zh-CN,补 en |
| GHE 支持 | 企业用户 | API base 可配置(设置面板) |
| PWA 离线壳 | 移动回访 | manifest + service worker 缓存壳与已分析数据 |

## 已知限制(透明声明)

- Issue 关闭时长中位数来自"最近更新的 100 个 issue"样本,大仓库存在近期偏差
- GitHub stats 接口冷缓存时返回 202,首次分析可能降级为采样(界面会注明)
- 评分为启发式估算,权重与归一化上限均写在 `js/score.js`,欢迎 issue 讨论
